# WP→Sanity移行 フェーズ3（画像アセット移行 / DNS切替準備）

## 目的

フェーズ2で移行した `wpImportedArticle` を対象に、WP外部参照の画像をSanity資産へ段階移行し、最終的なDNS切替に備える。

進捗表記:
- `[ ]` 未着手 / 未確定
- `[◯]` 完了 / 確定

---

## 現在進捗（2026-02-27）

- [◯] フェーズ3ドキュメントを作成
- [◯] スコープを確定（画像移行 + DNS切替準備）
- [◯] FooGalleryサムネイルの表示崩れを詳細画面UIで暫定解消（`data-src-fg` 補完 + 横並び調整）
- [ ] 画像移行スクリプト実装
- [ ] DNS切替前チェックリストの運用開始

状態:
- フェーズ2（全件移行）と並行して、フェーズ3は **設計着手**。

---

## スコープ

### 1) 画像アセット移行

- [ ] 対象定義
  - [ ] ヒーロー画像（`heroImageExternalUrl`）
  - [ ] 本文内画像（`legacyBodyHtml` の `img[src]`）
  - [ ] ギャラリー系（FooGallery短縮コード含む）の扱い方針
- [ ] 変換ルール定義
  - [ ] WP URL → Sanity Asset URL 対応表
  - [ ] 置換失敗時の記録方式
- [ ] スクリプト実装
  - [ ] ダウンロード
  - [ ] Sanity Upload
  - [ ] ドキュメント更新（URL置換）
  - [ ] dry-run / apply

### 2) 画像品質確認

- [ ] サンプル記事で表示確認（PC/スマホ）
- [ ] 画像欠落/リンク切れの検出
- [ ] 失敗記事の再実行フロー確立

### 3) DNS切替準備

- [ ] Vercel運用ルール確定（Preview/Production）
- [ ] canonical切替手順確定（旧WP → self）
- [ ] noindex/robots方針確定（切替前後）
- [ ] ロールバック手順確定

---

## レポート運用

- [ ] 画像移行対象件数の集計
- [ ] 移行成功/失敗CSVの出力
- [ ] 未解決課題を要修正リストへ集約

推奨レポート:
- `image-migration-targets-*.csv`
- `image-migration-failed-*.csv`
- `image-migration-retry-queue-*.csv`

---

## 完了条件

- [ ] 外部画像参照が許容範囲まで削減されている
- [ ] 重要記事の画像表示が安定している
- [ ] DNS切替手順とロールバック手順が文書化されている
- [ ] フェーズ4（リニューアル切替実施）へ進める状態

---

## 注記

- `featuredImage_api_denied` 記事は、フェーズ2の統合issues CSVを入力に優先対応する。
- FooGalleryは記事ごと差が大きいため、全自動化前提ではなく段階対応を許容する。
