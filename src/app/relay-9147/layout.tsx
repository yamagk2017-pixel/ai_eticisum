import type { Metadata } from "next";
import type { ReactNode } from "react";

export const metadata: Metadata = {
  title: "運用コンソール | IDOL CROSSING -アイドルと音楽の情報交差点-",
};

export default function Relay9147Layout({ children }: { children: ReactNode }) {
  return children;
}
